const fs = require("fs"); const t = fs.readFileSync("src/main/resources/static/index.html", "utf8"); const st = t.indexOf("<script>", 5000); const en = t.indexOf("</s"+"cript>", st); const lines = t.substring(st, en).split("\n"); let b = 0; for(let i=0; i<lines.length; i++) { let c = lines[i].replace(/\/\/.*/, "").replace(/\\/g,"").replace(/\"[^\"]*\"/g, "").replace(/\x27[^\x27]*\x27/g, "").replace(/\x60[^\x60]*\x60/g, ""); let open = (c.match(/\{/g)||"").length; let close = (c.match(/\}/g)||"").length; b += open - close; if(b < 0) { console.log("Drop at", i, lines[i]); console.log("Previous 10 lines:"); for(let j=Math.max(0, i-10); j<=i; j++) console.log(j, lines[j]); break; } }
